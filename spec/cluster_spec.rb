require 'spec_helper'

describe 'cluster' do

  before(:all) do
  end

  after(:all) do
  end

  it 'should bind a listen port for routes if configured'

  it 'should attempt to connect to another server if configured'

  it 'should properly route messages between different servers'

  it 'should properly route messages for distributed queues with consumers on different servers'

end
